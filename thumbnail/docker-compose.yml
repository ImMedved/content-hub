services:
  thumbnail:
    build: .
    container_name: smart-gallery-thumbnail
    restart: unless-stopped
    ports:
      - "8091:8080"
    environment:
      SERVER_PORT: 8080
      SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE: 50MB
      SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE: 50MB
